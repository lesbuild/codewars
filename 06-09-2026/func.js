function sortArray(arr) {
  return arr.sort((a, b) => a - b);
}

// Description:

// Given an array of integers, sort the array in ascending order and return the sorted array. If the array is empty, return an empty array.